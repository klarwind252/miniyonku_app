; ============================================================================
;  hwcheck_gw — 基板 受入確認用（4MB検証統一版・診断専用）
;  ★検証段階：手持ちの32E系(4MB・PSRAMなし)で統一。
; ============================================================================
[env:gw_check]
platform = espressif32
board = esp32dev                ; 32E系(WROOM/4MB)に素直に合う汎用定義
framework = arduino
monitor_speed = 115200
