; ============================================================================
;  server_post — GW→クラウド 送信確認（検証専用・GW1枚）
;  ⚠ src/secrets.example.h を secrets.h にコピーしてWiFi情報を入れること。
; ============================================================================
[env:gw]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
